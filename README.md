### Create a new environment before starting execution
```
conda create -p venv python==3.7 -y
```

### Activate the environment
```
conda activate venv
```

### Install the requirements for the project
```
pip install -r requirements.txt
```
